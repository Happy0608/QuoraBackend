const {Comment}=require('../models/index');

class CommentRepository{
    async createcomment(){
        const comment=await Comment.create({
            parent_id: dspj,
            parent: safd,
            user_id:notheirng
        })
    }
}